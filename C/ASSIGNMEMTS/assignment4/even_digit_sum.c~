#include<stdio.h>
void main()
{
	int no,rem,sum=0;
	printf("Enter no");
	scanf("%d",&no);
	while(no>0)
	{	
		rem=no%10;
		if(rem%2==0)
		{
		sum=sum+rem;
		}
		no=no/10;
	}
if(sum>0)
{
printf("sum of even digit is %d\n",sum);
}
else
{
printf("no even digits\n");
}
